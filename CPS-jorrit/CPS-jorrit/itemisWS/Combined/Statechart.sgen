GeneratorModel for create::python {

	const PROJECT : string = "Combined"

	const FOLDER : string = "src-gen"

	statechart model {

		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
			libraryTargetFolder = "src"
		}
	}
}
